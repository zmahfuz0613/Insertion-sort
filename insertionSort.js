function insertionSort(arr) {
  // 1. loop though arr starting at index 1
  // 2. save current value to a var
  // 3. compare current value to the value before if
  // 4. if current value is less then current value index - 1 swap places
  // 5. continue to compare current value to to all items that are in the array before it and swap there places
  // 6. if currentVal is greater then your current comparison it should stop looping
}

let arr = [2, 1, 5, 3, 6, -1, 12];
insertionSort(arr);

console.log(arr);
